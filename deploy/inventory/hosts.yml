---
# One host, deliberately. Both the tool-call rate limiter and the token
# verification cache live in process memory, so this must not be scaled
# horizontally: see docs/pi-deployment.md, "Known constraints".
browser_mcp:
  hosts:
    pi:
      ansible_host: raspberrypi.local
      ansible_user: pi
